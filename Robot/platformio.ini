; PlatformIO project config for the Rubik's cube solver robot firmware.
; Change `board` below if the robot uses a different Arduino (e.g. megaatmega2560, nanoatmega328, esp32dev).

[platformio]
src_dir = .

[env:uno]
platform = atmelavr
board = uno
framework = arduino
monitor_speed = 115200
